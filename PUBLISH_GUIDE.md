# 📦 发布到 NPM 指南

## ✅ 准备工作（已完成）

- ✅ 版本号已更新: `4.0.0` → `4.0.1`
- ✅ CHANGELOG.md 已更新
- ✅ 代码已重新编译
- ✅ .gitignore 已配置正确

## 🚀 发布步骤

### 1. 确认你已登录 npm

```bash
npm whoami
```

如果未登录，先登录：
```bash
npm login
```

### 2. 提交代码到 Git（可选但推荐）

```bash
git add .
git commit -m "v4.0.1: 修复工具列表请求失败问题"
git tag v4.0.1
git push
git push --tags
```

### 3. 发布到 npm

```bash
npm publish --access public
```

> **注意**: 由于包名是 `@xingyuchen/mysql-mcp-server`（scoped package），需要加上 `--access public` 参数

### 4. 验证发布

发布成功后，访问：
- npm 页面: https://www.npmjs.com/package/@xingyuchen/mysql-mcp-server
- 检查版本: `npm view @xingyuchen/mysql-mcp-server version`

### 5. 测试安装（可选）

```bash
# 在另一个目录测试安装
npm install -g @xingyuchen/mysql-mcp-server@4.0.1

# 或者本地测试
npm install @xingyuchen/mysql-mcp-server@4.0.1
```

## 📋 发布检查清单

在执行 `npm publish` 之前，确认：

- [ ] ✅ 版本号已更新
- [ ] ✅ CHANGELOG 已更新
- [ ] ✅ 代码已编译 (`npm run build`)
- [ ] ✅ package.json 的 `files` 字段正确（已配置为包含 `dist/**/*`）
- [ ] ✅ .gitignore 正确配置
- [ ] 🔐 没有包含敏感信息（密码、token 等）
- [ ] 📝 README.md 描述准确

## 🎯 这次发布的主要内容

### v4.0.1 - Bug 修复版本

**修复内容**:
- 修复了 `❌ 工具列表请求失败: Not connected` 错误
- 正确实现了 StreamableHTTP transport 使用方式
- 根据 MCP SDK 官方文档重构请求处理逻辑

**影响**:
- 所有 MCP 工具现在可以正常调用
- Header 预配置功能完全正常
- 不影响 API 接口，向下兼容

## 🔄 回滚方案（如果需要）

如果发布后发现问题，可以弃用该版本：

```bash
npm deprecate @xingyuchen/mysql-mcp-server@4.0.1 "存在问题，请使用 4.0.0 或更新版本"
```

## 📞 遇到问题？

常见问题：

1. **发布权限错误**: 确保你的 npm 账号有权限发布 `@xingyuchen` scope 的包
2. **版本冲突**: 确保该版本号之前没有发布过
3. **文件缺失**: 检查 package.json 的 `files` 字段是否正确

## 🎉 发布成功后

- 更新 GitHub Release（如果使用）
- 在 README 中更新安装命令（如果有变化）
- 通知用户更新（如果有社区）

---

## 快速发布命令（一键执行）

如果你确认所有检查都通过了，可以执行：

```bash
# 提交并发布
git add .
git commit -m "v4.0.1: 修复工具列表请求失败问题"
git tag v4.0.1
git push && git push --tags
npm publish --access public
```

**完成！** 🎊

